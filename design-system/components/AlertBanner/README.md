# AlertBanner

Banner discreto de alerta en la vista general, solo si existe una.

- Una línea, con quién la detectó y qué pasó; nunca diagnóstico.
- Terracota suave (`alert`), no rojo. Una sola a la vez; el resto va en la bandeja.
