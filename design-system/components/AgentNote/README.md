# AgentNote

Nota breve de un especialista en la junta de caso semanal; con `synthesis` es la síntesis de Iván, sobre su color.

- 2 a 3 líneas. Primera persona del agente, lenguaje simple.
- El avatar va siempre con `label` (nombre + IA + especialidad).
